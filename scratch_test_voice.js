import axios from 'axios';

async function run() {
  try {
    // Assuming backend runs on 5000 or similar? Let's check package.json of backend
    // Or we can just import the controller if we use ts-node
  } catch (e) {
    console.error(e);
  }
}
run();
